/**
 * Representing a password object.
 * Has a Name for identifying the website, the encrypted password and an URL
 */
export type PasswordInfo = {
  // id?: string
  name: string
  password: string
  url: string
}
